[project]
name = "coi-worker"
version = "0.1.0"
description = "Vercel Cron worker that renders Golden by Corgi ACORD 25 COIs"
requires-python = ">=3.9"
dependencies = [
    "pypdf>=4.0,<7",
    "pg8000>=1.30",
    "requests>=2.28",
]

[tool.vercel]
entrypoint = "api.render_queue:handler"
